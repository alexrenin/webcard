import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(() => ({
  container: {
    width: '100%',
    overflow: 'hidden',
    position: 'relative',
  },
}));

export default {
  useStyles,
};
